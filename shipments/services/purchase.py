def purchase_upload_session(upload_session_id: str, label_format: str) -> dict:
    # Lock shipments, enforce idempotency, transition session, create labels
    # Placeholder implementation
    return {'upload_session_id': upload_session_id, 'purchased': True, 'labels': []}
